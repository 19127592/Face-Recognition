- Run file test.py, train-face for training.

- Use some celebrities and students for traning.

- Accuracy is low as the dataset is not enough, the model is not well-trained

- There is a bug which terminal is freeze after import images, kill the terminate before continuing

- The images is saved at images_check.

- After register, run train-face.py to train the model

- Khi Login, xuất hiện cửa sổ Cam để bắt camera thì ta nhìn xuống terminal xem có load student id của người đã đăng kí không.
Nếu có tức chương trình đang nhận diện đúng, nhấn x để đóng cửa sổ Cam và hoàn tất check in.

- When login, a camera window will pop up, get your face before the camera then look at the terminal. If there is your ID on the terminal, the application is doing right.
